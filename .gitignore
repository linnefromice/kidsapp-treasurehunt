# Claude Code local files
CLAUDE.local.md

# Claude Code workspace deploy bookkeeping (local, drifts over time)
.claude/.deploy-state

# Flutter/Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# FVM Version Cache
.fvm/

# Secrets / signing — never commit (use GitHub Secrets / local-only files)
.env
.env.*
*.jks
*.keystore
key.properties
*.p12
*.pem
# Firebase config (App Distribution は CI のみ・SDK 非埋め込みのため本来不要)
google-services.json
GoogleService-Info.plist