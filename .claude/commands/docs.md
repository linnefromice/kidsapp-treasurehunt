---
description: documentation-lookupスキルのレガシースラッシュエントリーシム。スキルを直接使用することを推奨します。
---

# Docsコマンド（レガシーシム）

`/docs` をまだ使用している場合のみ使用してください。メンテナンスされているワークフローは `skills/documentation-lookup/SKILL.md` にあります。

## 正規サーフェス

- `documentation-lookup` スキルを直接使用することを推奨します。
- このファイルは互換性のエントリーポイントとしてのみ保持してください。

## 引数

`$ARGUMENTS`

## 委譲

`documentation-lookup` スキルを適用します。
- ライブラリまたは質問が不足している場合は、不足部分を尋ねてください。
- トレーニングデータではなくContext7を通じたライブドキュメントを使用します。
- 現在の回答と必要最小限のコード/例のサーフェスのみを返します。
